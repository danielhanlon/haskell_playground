<!--

> {-# LANGUAGE CPP #-}

-->

<h2><a name="rqdataparsing">Handling non-String request parameters</a></h2>

<p>So far we have only tried to look up <code>String</code> values
using <code>look</code>. But the <code>RqData</code> module provides a
variety of ways to work with values besides <code>Strings</code>.</p>

#include "RqDataRead.lhs"
#include "RqDataCheck.lhs"
#include "RqDataCheckOther.lhs"
